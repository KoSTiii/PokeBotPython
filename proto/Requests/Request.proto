syntax = "proto3";
package POGOProtos.Requests;

import public "Requests/RequestType.proto";

message Request {
  POGOProtos.Requests.RequestType request_type = 1;
  RequestMessage request_message = 2;
  
  message RequestMessage {
	bytes message = 1;
  }
}
